#include <stdio.h>
#include <iostream>
#include <stdlib.h>
#include <algorithm>
#include <string>
#define rep(i,a,n) for(i=a; i<n; i++)
#define in(x)  scanf("%d",&x)
#define out(y)  printf("%d ",y)
using namespace std;
int main()
{
	int i,j,k,n,t,arr[200],prev,temp;
	cin >> t;	
	while(t--)
	{
		rep(i,0,200)
			arr[i]=0;
		cin >> n;
		k=0,arr[0]=1;
		for(i=1;i<=n;i++)
		{
			k=0;
			while()
			{	
				temp=i*arr[k];
				while(temp>=10)
				{
					
				}
			}
		}

	} 
	return 0;
}
